a=input("Enter a number: ")
b=input("Enter another number: ")
print(a+b) # This will concatenate the two inputs as strings, not add them as 
#numbers. To add them as numbers, we need to convert them to integers or floats.
# So this will not work you can see the correct code in Input Handling 2.py