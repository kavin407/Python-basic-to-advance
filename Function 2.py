def great(name):
    print("Hello, " + name + ". How are you?")
great()
#This code will give an error because the function great() is defined to take one argument (name), but it is called without any arguments. 
# To fix this, you can either provide an argument when calling the function or 
# modify the function definition to not require an argument. For example, you can call the function with an argument
# like this